<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
<url><loc>{{ request.host_url }}</loc><changefreq>daily</changefreq><priority>1.0</priority></url>
<url><loc>{{ request.host_url }}shop</loc><changefreq>daily</changefreq><priority>0.9</priority></url>
{% for p in products %}
<url><loc>{{ request.host_url }}shop/product/{{ p.slug }}</loc><changefreq>weekly</changefreq><priority>0.8</priority></url>
{% endfor %}
{% for c in categories %}
<url><loc>{{ request.host_url }}shop/category/{{ c.slug }}</loc><changefreq>weekly</changefreq><priority>0.7</priority></url>
{% endfor %}
{% for pg in pages %}
<url><loc>{{ request.host_url }}page/{{ pg.slug }}</loc><changefreq>monthly</changefreq><priority>0.5</priority></url>
{% endfor %}
</urlset>
